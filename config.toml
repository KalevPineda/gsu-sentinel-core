cloud_url = "http://192.168.0.11:8080"
turbine_token = "turbine_104_park_15"

[limits]
max_temp_trigger = 50.0 
collection_duration_sec = 10 
# Tiempo de espera al llegar a un extremo (GSU 1 o 2) antes de volver
scan_wait_time_sec = 5
# Velocidad del barrido: Grados por frame (aprox 25 FPS)
# 0.5 grados * 25 fps = 12.5 grados por segundo (Velocidad suave)
pan_step_degrees = 0.2

[network]
zmq_endpoint = "tcp://127.0.0.1:5555"
mqtt_broker = "127.0.0.1"
http_port = 3000